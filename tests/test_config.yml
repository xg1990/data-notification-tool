connected_services:
  test_sqlite:
    class_name: SQLSource
    config:
  test_action:
    class_name: ConsoleAction
    config:
notification_groups:
  admin:
    - service: test_action
alerts:
  - conditions:
      - service: test_sqlite
        query_file: ./
    notify:
      - group: admin
on_failure:
  - group: admin
